#!/bin/bash
# This is a bash script.
# Put your description below this line and code below that.
# 
echo
echo "Running my custom lb config and build..."
echo 'spike' | sudo -S -v
sleep 1

<<<<<<< HEAD
=======
# Full clean of working dir
>>>>>>> 8f833b6 (reset)
sudo lb clean --all --cache --chroot --binary --purge --remove --stage --source

# Define dirs and clean and create
RT_DIR="$HOME/MSI_live-build"
MAIN_CONF="$RT_DIR/config"
MY_CONF="$RT_DIR/my_configs"
if [[ -d $MAIN_CONF ]]; then
	rm -rf $MAIN_CONF
fi
CHROOT_INCL="$MAIN_CONF/includes.chroot"
if [[ ! -d $CHROOT_INCL ]]; then
	mkdir -p $CHROOT_INCL
fi

cd $RT_DIR

<<<<<<< HEAD
lb config
sleep1

=======
# Create build config
lb config
sleep 1


echo
echo "Creating sources for live build from my apt/sources.list.d..."
sleep 1

# Modernize sources and add .pref file from my working linux
mkdir -p $MAIN_CONF/archives
cp $MY_CONF/sources.list $MAIN_CONF/archives/sources.list.chroot
cp $MY_CONF/sources.list $MAIN_CONF/archives/sources.list.binary
# My apt prefs
cp $MY_CONF/my_preferences.pref $MAIN_CONF/archives/

echo
echo "Creating config folders and adding my custom scripts..."
sleep 1

# Add msifland as user and put in sudo
mkdir -p $MAIN_CONF/hooks/live
mkdir -p $MAIN_CONF/hooks/normal
cp $MY_CONF/02-add-user.chroot $MAIN_CONF/hooks/live/
# Add me to sudoers
mkdir -p $CHROOT_INCL/etc/sudoers.d
cp $MY_CONF/msifland $CHROOT_INCL/etc/sudoers.d/

# Root password
mkdir -p $CHROOT_INCL/lib/live/config
cp $MY_CONF/2000-passwd $CHROOT_INCL/lib/live/config/

# Lightdm, msifland auto login
mkdir -p $CHROOT_INCL/etc/lightdm
cp $MY_CONF/lightdm.conf $CHROOT_INCL/etc/lightdm/

# Skel directory adds user files
mkdir -p $CHROOT_INCL/etc/skel/desktop
cp $MY_CONF/Debian-Install.desktop $CHROOT_INCL/etc/skel/desktop
rsync -a --exclude=".git" $HOME/scripts $CHROOT_INCL/etc/skel/
rsync -a --exclude=".git" $HOME/Documents $CHROOT_INCL/etc/skel/
rsync -a --exclude=".git" $HOME/pkg_bkups $CHROOT_INCL/etc/skel/
rsync -a --exclude=".git" $HOME/Modules-Apps $CHROOT_INCL/etc/skel/
rsync -a --exclude="chrome-remote-desktop" --exclude="chromium" --exclude="google-chrome" --exclude="skypeforlinux" --exclude="Code" --exclude="variety" $HOME/.config $CHROOT_INCL/etc/skel/
rsync -a --exclude="share/applications" --exclude="share/baloo" --exclude="share/akonadi" --exclude="share/lutris" --exclude="share/Steam" --exclude="share/Trash" --exclude="share/data" $HOME/.local $CHROOT_INCL/etc/skel/
rsync -a --exclude="chromium" --exclude="chrome-remote-desktop" --exclude="mozilla" --exclude="tracker" --exclude="wine*" --exclude="google-chrome" --exclude="at-spi*" $HOME/.cache $CHROOT_INCL/etc/skel/
rsync -a $HOME/.Xresources $CHROOT_INCL/etc/skel/
rsync -a $HOME/.bash* $CHROOT_INCL/etc/skel/
rsync -a $HOME/.blerc $CHROOT_INCL/etc/skel/
rsync -a $HOME/.nanorc $CHROOT_INCL/etc/skel/

# My packages to install during build
mkdir -p $MAIN_CONF/package-lists
cp $MY_CONF/my_packs.list.chroot $MAIN_CONF/package-lists/
sleep 1


echo
echo "Running configs..."
sleep 1
# Include debian installers
>>>>>>> 8f833b6 (reset)
lb config --debian-installer netinst
sleep 1
lb config --debian-installer-gui true
sleep 1
lb config --debian-installer-distribution testing
sleep 1
<<<<<<< HEAD
lb config --system live
sleep 1
=======
# Distro e.g. testing, stable, unstable
lb config --distribution testing
sleep 1
# Live system or actual system
lb config --system live
sleep 1
# Include firmware, also tied with arhcive-areas for non-free
>>>>>>> 8f833b6 (reset)
lb config --firmware-binary true
sleep 1
lb config --firmware-chroot true
sleep 1
lb config --image-name MSI_live-image
sleep 1
<<<<<<< HEAD
=======
# Whether to use security repos
>>>>>>> 8f833b6 (reset)
lb config --security true
sleep 1
lb config --updates true
sleep 1
lb config --archive-areas "main contrib non-free-firmware non-free"
sleep 1
lb config --parent-archive-areas "main contrib non-free-firmware non-free"
sleep 1
<<<<<<< HEAD
lb config --build-with-chroot true
sleep 1
lb config --bootappend-live "boot=live components persistence username=msifland"

mkdir -p $MAIN_CONF/hooks
mkdir -p $MAIN_CONF/hooks/live
mkdir -p $MAIN_CONF/hooks/normal
cp $MY_CONF/02-add-user.chroot $MAIN_CONF/hooks/live/
cp $MY_CONF/02-add-user.chroot $MAIN_CONF/hooks/normal/

mkdir -p $CHROOT_INCL/etc/lightdm
cp $MY_CONF/lightdm.conf $CHROOT_INCL/etc/lightdm/

mkdir -p $CHROOT_INCL/etc/sudoers.d
cp $MY_CONF/msifland $CHROOT_INCL/etc/sudoers.d/

mkdir -p $CHROOT_INCL/etc/skel/desktop
cp $MY_CONF/Debian-Install.desktop $CHROOT_INCL/etc/skel/desktop
rsync -a --exclude=".git" $HOME/scripts $CHROOT_INCL/etc/skel/
rsync -a --exclude=".git" $HOME/Documents $CHROOT_INCL/etc/skel/
rsync -a --exclude=".git" $HOME/pkg_bkups $CHROOT_INCL/etc/skel/
rsync -a --exclude=".git" $HOME/Modules-Apps $CHROOT_INCL/etc/skel/
rsync -a $HOME/.bash* $CHROOT_INCL/etc/skel/
rsync -a $HOME/.blerc $CHROOT_INCL/etc/skel/
rsync -a $HOME/.nanorc* $CHROOT_INCL/etc/skel/

mkdir -p $MAIN_CONF/package-lists
cp $MY_CONF/my_packs.list.chroot $MAIN_CONF/package-lists/
sleep 1
=======
# Build in chroot env, always keep true
lb config --build-with-chroot true
sleep 1
# Remove uneeded files after build
lb config --apt-indices false
sleep 1
lb config --apt-options "--yes --allow-downgrades --allow-remove-essential --allow-change-held-packages"
sleep 1
# Kernel params
lb config --bootappend-live "boot=live components persistence username=msifland"
>>>>>>> 8f833b6 (reset)
